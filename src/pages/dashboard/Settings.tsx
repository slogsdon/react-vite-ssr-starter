// This is the Dashboard Settings page component.
// It renders the content for the dashboard settings route ('/dashboard/settings').

export default function Settings() {
  return <h1>Dashboard Settings</h1>;
}
